# Copy to .env and adjust as needed.
# These are optional; sensible defaults exist for local development.
SECRET_KEY=change-me
DATABASE_URL=sqlite:///data/detail.db
# Auto end-of-day runs daily at this local time (HH:MM) if no one ended the day.
# AUTO_END_DAY_TIME=23:50
# PORT=5000
